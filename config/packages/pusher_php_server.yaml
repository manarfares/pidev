services:
    Pusher\Pusher:
        public: true
        arguments:
            - '%env(PUSHER_APP_KEY)%'
            - '%env(PUSHER_APP_SECRET)%'
            - '%env(PUSHER_APP_ID)%'
            - 
                cluster: '%env(PUSHER_APP_CLUSTER)%'
                useTLS: true
